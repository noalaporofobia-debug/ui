local NovaUI = loadstring(game:HttpGet("tu_link"))()

local Window = NovaUI:CreateWindow({
    Title = "Nova",
    Size = UDim2.fromOffset(650,450)
})

local Main = Window:AddTab("Home")

Main:AddButton({
    Text = "Test",
    Callback = function()
        print("clicked")
    end
})

Main:AddToggle({
    Text = "Enabled",
    Callback = function(v)
        print(v)
    end
})
